# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: catalog/search.spec.ts >> Search >> searching via the header box returns results
- Location: tests/catalog/search.spec.ts:4:7

# Error details

```
Error: expect(page).toHaveTitle(expected) failed

Expected pattern: /results found/i
Received string:  "Just a moment..."
Timeout: 8000ms

Call log:
  - Expect "toHaveTitle" with timeout 8000ms
    - locator resolved to <html lang="en">…</html>
    - unexpected value "Verifying your connection..."
    19 × locator resolved to <html lang="en" dir="ltr">…</html>
       - unexpected value "Just a moment..."

```

```yaml
- heading "Your connection needs to be verified before you can proceed" [level=1]
- main:
  - heading [level=2]
  - paragraph
```

# Test source

```ts
  1  | import { test, expect } from '../../src/fixtures/pageFixtures';
  2  | 
  3  | test.describe('Search', () => {
  4  |   test('searching via the header box returns results', async ({ homePage, page }) => {
  5  |     await homePage.goto();
  6  |     await homePage.header.searchFor('jacket');
  7  | 
  8  |     await expect(page).toHaveURL(/\/search\?.*q=jacket/);
> 9  |     await expect(page).toHaveTitle(/results found/i);
     |                        ^ Error: expect(page).toHaveTitle(expected) failed
  10 |   });
  11 | 
  12 |   test('searching for a nonsense query returns no results', async ({ searchResultsPage }) => {
  13 |     await searchResultsPage.goto('zzzznonexistentproductzzzz');
  14 |     await expect(searchResultsPage.page).toHaveTitle(/0 results found/i);
  15 |     expect(await searchResultsPage.resultCount()).toBe(0);
  16 |   });
  17 | });
  18 | 
```